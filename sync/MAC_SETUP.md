# Mac Sync

Clone:

```bash
git clone https://github.com/selimdelikara-lgtm/odeme-paneli.git
cd odeme-paneli
npm install
```

Before work:

```bash
./sync/mac-sync.sh
```

After work:

```bash
./sync/mac-sync.sh
```

Windows:

```powershell
.\sync\windows-sync.ps1
```
